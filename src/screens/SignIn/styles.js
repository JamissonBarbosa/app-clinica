import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#87CEFA',
      alignItems: 'center',
    },
    textLogin :{
      marginTop: '40px',
      fontWeight: 'bold',
      fontSize: '80px'
    }
})